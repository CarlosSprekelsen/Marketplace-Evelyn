name: marketplace_app
description: Marketplace de limpieza por horas - App móvil
publish_to: 'none'
version: 1.2.0+3

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State management
  flutter_riverpod: ^3.2.1
  riverpod_annotation: ^4.0.2

  # HTTP client
  dio: ^5.9.1

  # Secure storage
  flutter_secure_storage: ^10.0.0

  # Push notifications
  firebase_core: ^4.4.0
  firebase_messaging: ^16.1.1

  # URL launcher (calls, WhatsApp)
  url_launcher: ^6.3.2

  # Maps
  google_maps_flutter: ^2.14.2
  google_maps_flutter_android: ^2.19.0
  google_maps_flutter_platform_interface: ^2.14.1

  # Routing
  go_router: ^17.1.0

  # UI
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.11.1
  riverpod_generator: ^4.0.3
  # custom_lint removed due to analyzer conflict with riverpod_lint
  riverpod_lint: ^3.1.3

flutter:
  uses-material-design: true
